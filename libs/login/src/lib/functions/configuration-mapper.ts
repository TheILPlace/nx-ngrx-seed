// import { LoginConfiguration } from '../interfaces/login-configuration.interface';

// export function mapLoginConfiguration(config: any): LoginConfiguration {

//     const loginConfiguration = new LoginConfiguration();

//     Object.entries(loginConfiguration).forEach(([key, value]) => {
//         if (config.hasOwnProperty(key)) {
//             loginConfiguration[key] = config[key];
//         }
//     });

//     return loginConfiguration;

// }